// MySQL
const mysql = {
  common: '/common/backend/mysql',
}

// Redis
const redis = {
  common: '/common/backend/redis',
}

// Server
const server = {
  common: '/common/backend/server',
}

module.exports = {
  mysql,
  redis,
  server,
}
